NumPy 数组对象 NumPy中的ndarray是一个多维数组对象，该对象由两部分组成:
    实际的数据;
    描述这些数据的元数据。
大部分的数组操作仅仅修改元数据部分，而不改变底层的实际数据。

NumPy数组的下标从0开始。
同一个NumPy数组中所有元素的类型必须是相同的。

Pandas 与NumPy 差不多 但更强大
与NumPY 不同Pandas 有索引

Pandas 有 DataFrame 二维结构 每一列有索引 可以不同类型

有点像表